@model FastReport.Web.WebReport
@{
    ViewData["Title"] = "Visualizar Relatório - FastReport";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid">
    <div class="row">
        <div class="col-md-12">
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h3><i class="fas fa-eye"></i> Visualizar Relatório</h3>
                <div>
                    <a href="@Url.Action("Index")" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Voltar aos Relatórios
                    </a>
                </div>
            </div>

            <div class="card">
                <div class="card-body p-0">
                    <!-- FastReport WebReport Control -->
                    @{
                        Model.Width = "100%";
                        Model.Height = "600px";
                    }
                    @(Model.Render())
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    /* Customizar o estilo do WebReport */
    .fr-webreport {
        min-height: 600px;
        border: 1px solid #ddd;
        border-radius: 0.375rem;
    }

    /* Esconder elementos desnecessários */
    .card-body {
        padding: 0 !important;
    }

    /* Ajustar toolbar do FastReport */
    .fr-toolbar {
        background: #f8f9fa;
        border-bottom: 1px solid #dee2e6;
        padding: 0.5rem;
    }
</style>
